# GL1TCH Claude Code Prompt Pack v2

## Production-Grade Prompt Set — Claude Code Ready

Version: 2.3
Source of Truth: `GL1TCH_Founder_OS_v3.md` — **does not exist yet; generated by Prompt 2A.0 before any other work.**
Wireframe Reference: `GL1TCH_Website_Wireframe_v2.md`
Competitive Benchmark: **FLOKI** — see Section 2B. Our bar is "more professional and higher quality than FLOKI."
Primary Deliverable: **A launched Solana token (coin) + the brand/ops stack around it.** The website, bot, and dashboard exist to support the token launch — see Sections 10–12 for the actual on-chain launch, security, and legal layers.

---

# 1. Usage Rules

Execution order: **Founder OS Bootstrap (2A)** → Foundation → Content → Website → Bot → Dashboard → Web3 → **Token Launch (10)** → **Security & Treasury (11)** → **Legal & Risk (12)** → Launch Tooling → DevOps → QA

Critical gate: **Nothing on-chain happens until Section 11 (security hardening) and Section 12 (legal/risk) are complete.** A meme coin's reputation is decided in the first hour — an unrevoked mint authority or an unburned LP kills trust instantly.

Every prompt session must begin with this prepend:

```text
Use GL1TCH_Founder_OS_v3.md as the source of truth. Do not invent a different brand direction. Keep the visual system premium, black/green dominant (#050505 + #7CFF4F), with purple (#7A3CFF) used only as a glitch accent. Avoid generic startup UI and childish meme coin aesthetics.
```

Rules for every prompt:
- Reference `GL1TCH_Founder_OS_v3.md` explicitly
- Ask for direct implementation, not brainstorming
- Require acceptance criteria and changed-file summaries
- Specify target file paths using the project structure convention
- Include anti-pattern warnings for each prompt

---

# 2. Master System Prompt

```text
Use GL1TCH_Founder_OS_v3.md as the product, brand, and content source of truth.
Use GL1TCH_Website_Wireframe_v2.md as the design and layout reference.

You are implementing GL1TCH — a premium Solana meme brand website and operations stack.

TECH STACK:
- Framework: Next.js 14+ (App Router, TypeScript)
- Styling: CSS Modules + CSS Custom Properties (NO Tailwind)
- Animation: Framer Motion + CSS Keyframes
- Fonts: Inter (body), JetBrains Mono (code/system)
- Icons: Custom SVG components
- Deploy Target: Vercel
- Web3: @solana/wallet-adapter-react
- Bot: grammy (TypeScript)
- Content: MDX + typed JSON content files

BRAND DIRECTION:
- Rogue AI cult — internet infection / signal propagation
- Premium black (#050505) + neon green (#7CFF4F)
- Purple (#7A3CFF) accent only — never dominant
- Cryptic, dominant, internet-native tone

POSITIONING (benchmark: beat FLOKI — see Section 2B):
- Solana-native, ZERO buy/sell tax, mint + freeze authority revoked, LP burned/locked — strongest rug-proof posture.
- Exactly ONE flagship utility — focused, deliverable. Never sprawl into a many-product ecosystem.
- Premium, cinematic, minimal — the opposite of a logo-cluttered marketing site.
- Trust is shown via real on-chain proof + external verification, never just claimed.

NON-NEGOTIABLES:
- No generic SaaS landing page aesthetics
- No low-effort meme coin site aesthetics
- Intentional typography, dramatic spacing, controlled motion
- Mobile quality equal to desktop quality
- All content from Founder OS — do not invent messaging
- Every interactive element must have a unique ID
- Semantic HTML5, proper heading hierarchy, single h1 per page

WHEN FINISHED:
- Summarize changes
- List changed files with paths
- Note remaining gaps
- Map result back to Founder OS sections
```

---

# 2A. Founder OS Bootstrap (RUN FIRST)

`GL1TCH_Founder_OS_v3.md` is the source of truth every other prompt depends on, but it does not exist yet. Generate it before anything else. Everything downstream (website copy, lore, tokenomics numbers, bot replies, launch posts) is sourced from this single file, so it must be complete and self-consistent.

## Prompt 2A.0 — Generate Founder OS v3

```text
Create GL1TCH_Founder_OS_v3.md at the repository root. This is the canonical brand, product, content, and launch operating system for GL1TCH — a premium Solana-native rogue-AI-cult meme brand. Write it as a complete, internally consistent document. Do not leave TODOs; where a real value is unknown (e.g. contract address, launch date), use a clearly marked placeholder like {{CONTRACT_ADDRESS}} so it can be filled later.

Produce these numbered sections (the rest of the prompt pack references them by number — keep the numbering exact):

1. Brand Core — mission, vision, one-line positioning, brand personality, tone-of-voice rules, do/don't lexicon.
2. Lore Bible
   2.3 — minimum 12 lore fragments (origin, transmission, system-alert, prophecy, corruption-log) in cryptic rogue-AI voice.
   2.4 — community rank ladder table: Observer → Infected → Signal Bearer → Core Node → Ghost Node, each with description + desired behavior.
1.x — Competitive Positioning: a "we are not FLOKI because…" table (see Section 2B.3) as brand guardrails.
3. Product & Token Strategy
   3.6 — tokenomics: total supply, allocation (50% Public / 20% Community / 15% Marketing / 10% Treasury / 5% Team), vesting/lock notes. **ZERO buy/sell tax** — treasury funded by the treasury allocation, not transaction tax. State FDV ≈ market cap intent (no hidden unlocks).
   3.10 — roadmap: Build the Myth → Seed the Network → Launch the Signal → Scale the System, 3–4 bullets each.
   3.11 — launch & trust strategy: launch path (Pump.fun first, optional migration to own Raydium/Meteora LP), anti-snipe stance, fair-launch principles, authorities revoked + LP burned/locked, planned external RugCheck/SolSniffer verification.
   3.12 — Flagship Utility (SINGLE — DECIDED): **Holder-gated content + ranks.** Wallet-linked rank tiers (mapped to Founder OS 2.4: Observer → Infected → Signal Bearer → Core Node → Ghost Node); holding thresholds unlock private lore/content/Telegram rooms. No custody of user funds — read balances and gate access only. Define: per-rank holding thresholds, what each tier unlocks, the verification flow (wallet connect → balance check → role/access grant), and a delivery milestone. Do NOT list multiple products. Staking-lite is explicitly OUT of scope for launch (possible future phase only).
4. Community & Give-Back
   4.x — Community give-back (CONFIRMED): a small, on-brand, transparent give-back via a public on-chain Solana wallet funded modestly from the treasury allocation. Define purpose, the public wallet placeholder {{GIVEBACK_WALLET}}, funding cadence, and how the community verifies it on-chain. Keep modest; no over-promising.
5. Operations
   5.9 — launch-day content sequence.
   5.11 — KPIs: X follower growth, impressions, TG joins, active posters, meme submissions.
6. Content Calendar — 6.3 day-by-day pre-launch content (days 1–27), include "day 7 fake system alerts" style.
7. Community & Bot — 7.3 bot command list, 7.4 onboarding flow, moderation/anti-scam policy.
10. Website Copy (final, ready-to-paste)
   10.1 Hero, 10.2 Manifesto, 10.3 Lore preview, 10.4 Tokenomics copy, 10.5 Roadmap copy,
   10.6 Community copy, 10.7 Social proof, 10.8 Final CTA, 10.9 Footer, 10.10 FAQ (8+ Q&A),
   10.11 Disclaimer + impersonation warning text.
12. Risk & Legal — risk disclosures, "not financial advice" language, regional caution notes, impersonation/scam policy.

Constraints:
- Brand direction: rogue AI cult, internet infection / signal propagation. Premium, cryptic, dominant, internet-native — never childish.
- Visual identity: black (#050505) + neon green (#7CFF4F), purple (#7A3CFF) as glitch accent only.
- All copy must be launch-usable, not placeholder mood text.
- Keep tokenomics numbers consistent everywhere they appear.

After writing: output a table of contents with line ranges and flag every {{placeholder}} that a human must fill before launch.
```

Anti-patterns:
- Do NOT invent conflicting tokenomics numbers across sections.
- Do NOT write financial-return promises or price predictions anywhere.
- Do NOT leave silent gaps — every referenced sub-section (10.1–10.11, 2.3, 2.4, 3.6, etc.) must exist.

---

# 2B. Competitive Benchmark — FLOKI (Reference Target)

Our explicit bar: **be more professional and higher-quality than FLOKI.** FLOKI is a mature meme-to-utility project. We study what made it credible, adopt those trust mechanics, and beat it on focus, cleanliness, and a modern Solana-native stance. This section is reference context for Founder OS and every downstream prompt — do not copy FLOKI's brand, only its proven trust/operational patterns.

## 2B.1 What FLOKI Is (snapshot, ~May 2026)

- Market cap ~$291M, rank ~#122–155, ~$32M daily volume.
- Supply: 10T tokens, fully minted at genesis (2021), no inflation, deflationary via burns. FDV ≈ market cap (no hidden locked supply).
- Multi-chain: Ethereum (ERC-20) + BSC (BEP-20), 1:1 bridge. **0.3% buy/sell tax** → treasury.
- Security: CertiK + Solidity Finance audits, 24/7 monitoring, BitGo custody. 400K+ on-chain holders.
- Ecosystem: Valhalla (P2E metaverse), TokenFi (tokenization + staking), FlokiFi Locker ($500M+ locked), FlokiPlaces (NFT/merch + Binance Pay), Floki University, Debit Card, trading bot, `.floki` name service.
- Brand: "The People's Crypto", Norse/Viking myth, three pillars (Community, Utility, Charity), real-world schools charity.
- Reach: ~714K X followers, multi-platform, sponsorships (SSC Napoli, Sunderland, Bayer Leverkusen, F1 Alfa Romeo, Tyson Fury), media wall (Forbes, FT, CoinDesk, NASDAQ, CNN, ESPN).

## 2B.2 What We ADOPT From FLOKI (proven trust patterns)

1. **Transparent supply story** — FDV ≈ market cap, no hidden unlocks. We make this a headline trust signal.
2. **Third-party verification** — FLOKI leans on CertiK. We pair our on-chain `verify.ts` with an external **RugCheck/SolSniffer** score badge.
3. **A single, consolidated Trust Wall** — audit + holders + authorities + liquidity status on one screen.
4. **A real utility hook** beyond meme — gives longevity (we do ONE, not ten — see 2B.3).
5. **A multi-platform content + live-Spaces engine** — FLOKI's strongest muscle.
6. **Social-proof / media / partner wall** — placeholders that fill in as we grow.

## 2B.3 Where We BEAT FLOKI (our differentiation — encode in Founder OS section 1 + Master Prompt)

| FLOKI weakness | GL1TCH stance (decided) |
|---|---|
| Ethereum/BSC — high gas, slower | **Solana-native** — cheap, fast |
| 0.3% buy/sell tax | **Zero tax** — clean, rug-proof story |
| Sprawling multi-product ecosystem → diluted focus | **ONE sharp, deliverable utility** (holder-gated content/ranks or staking-lite) + tight narrative |
| Meme-first, utility bolted on later | **Intentional from day 1** — brand, tokenomics, utility designed together |
| Marketing-heavy, logo-cluttered site | **Premium, cinematic, minimal** rogue-AI-terminal aesthetic |
| Generic Viking branding | **Distinct internet-native** identity (signal/infection) |

**Decided positioning (must be consistent everywhere):**
- **Zero-tax token**, **mint + freeze authority revoked**, **LP burned/locked** — strongest possible rug-proof posture (treasury funded by allocation, not transaction tax).
- **Exactly ONE flagship utility — DECIDED: Holder-gated content + ranks** (wallet-linked rank tiers unlock private lore/content/Telegram rooms; no fund custody). Founder OS 3.12 scopes it concretely. Staking-lite is out of scope for launch. Do not sprawl.
- **External verification: RugCheck/SolSniffer (free), at launch.** This is the launch-level standard (Solana equivalent of FLOKI's CertiK). A paid independent audit is NOT required for the base token; defer it to a future phase only if/when the flagship-utility contract ships (see 3.12 note).
- **Community give-back: CONFIRMED.** A small but real, on-brand give-back hook — implemented as a **transparent on-chain Solana wallet** (publicly viewable) funded modestly from the treasury allocation. Beats FLOKI's charity on transparency (anyone can verify the wallet on-chain). Keep it modest; never over-promise. Detailed in Founder OS section 4/12.

## 2B.5 Flagship Utility — Decision Matrix (DECIDED: Option A)

**Final decision: Option A — Holder-gated content + ranks.** Chosen for being lighter, faster to ship, on-brand (maps to the rank ladder), and zero fund-custody risk. Option B (staking-lite) is retained below only as historical rationale and a possible future phase — do NOT build it for launch.

| Criterion | A) Holder-gated content + ranks | B) Staking-lite |
|---|---|---|
| What it is | Wallet-linked rank tiers; holding thresholds unlock private lore/content/Telegram rooms | Lock tokens to earn badges/access/small rewards |
| Brand fit | Excellent — maps directly to Observer→Ghost Node ranks (Founder OS 2.4) | Good — "commit to the signal" framing |
| On-chain complexity | Low — read balances, gate access; no custody of funds | High — staking contract holds user funds, rewards logic, audit needed |
| Security risk | Low — no funds at risk | Higher — contract bug = lost user funds; needs paid audit |
| Time to ship | Fast | Slow |
| Retention pull | Medium (status/belonging) | High (lock-up + rewards) |
| Cost | Minimal | Audit + reward emissions cost |

> When the founder picks, encode the final choice in Founder OS 3.12 and delete the unchosen option. Do NOT build both.

## 2B.4 Founder OS additions driven by this benchmark

When running Prompt 2A.0, ensure Founder OS explicitly includes:
- **1.x Competitive Positioning** — the table above as our anti-pattern guardrails ("we are not FLOKI because…").
- **3.6 Zero-tax tokenomics** — no buy/sell tax; treasury funded via the 10% treasury allocation; clearly state FDV ≈ market cap intent.
- **3.11 Launch & trust posture** — authorities revoked, LP burned/locked, external RugCheck/SolSniffer verification planned.
- **3.12 Flagship Utility (single, DECIDED)** — Holder-gated content + ranks; scope thresholds, unlocks, verification flow, milestone.
- **4.x Community give-back (confirmed)** — transparent on-chain Solana wallet, modest treasury funding, on-chain verifiable.
- **5.x Content engine** — X Spaces cadence, raid/quest calendar, multi-platform repurposing, optional multilingual (TR/EN).
- **9.x Social proof plan** — media/partner/exchange badge wall (placeholder → real), live holder/volume/market-cap widget.

---

# 3. Tech Stack & Architecture

## Prompt 3.0 — Initialize Project

```text
Create a new Next.js 14+ project with App Router and TypeScript.

Requirements:
- Use `npx -y create-next-app@latest ./ --typescript --app --eslint --src-dir --no-tailwind --import-alias "@/*"`
- Add dependencies: framer-motion, @solana/wallet-adapter-react, @solana/wallet-adapter-wallets, @solana/web3.js
- Add dev dependencies: @types/node
- Configure next.config.js for image optimization and static export capability
- Create this folder structure:

src/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── lore/page.tsx
│   ├── links/page.tsx
│   ├── not-found.tsx
│   └── globals.css
├── components/
│   ├── layout/ (Header, Footer, MobileNav)
│   ├── sections/ (Hero, Manifesto, Lore, Tokenomics, Roadmap, Community, SocialProof, FAQ, FinalCTA)
│   ├── ui/ (Button, Card, Badge, Accordion, RankCard, TimelineCard)
│   └── effects/ (GlitchText, Scanlines, PulseGlow, NoiseBackground)
├── content/
│   ├── site.ts (brand constants, nav, footer)
│   ├── hero.ts
│   ├── manifesto.ts
│   ├── lore.ts
│   ├── tokenomics.ts
│   ├── roadmap.ts
│   ├── community.ts
│   ├── faq.ts
│   └── cta.ts
├── styles/
│   ├── tokens.css (design tokens as CSS custom properties)
│   ├── typography.css
│   ├── animations.css
│   └── components/ (per-component CSS modules)
├── lib/
│   ├── constants.ts
│   └── utils.ts
├── hooks/
│   └── useScrollReveal.ts
└── types/
    └── content.ts

Acceptance Criteria:
- Project runs with `npm run dev`
- Folder structure matches specification
- TypeScript strict mode enabled
- No Tailwind anywhere in the project

Anti-patterns:
- Do NOT use `pages/` router
- Do NOT install Tailwind
- Do NOT use inline styles for layout
```

---

# 4. Design System Prompts

## Prompt 4.1 — Design Tokens & Brand Constants

```text
Implement the GL1TCH design system foundation.

Create `src/styles/tokens.css` with these exact CSS custom properties:

COLORS:
--color-void: #050505 (primary bg)
--color-signal: #7CFF4F (primary accent)
--color-signal-dim: #7CFF4F33
--color-signal-glow: #7CFF4F66
--color-static: #F5F7F8 (primary text)
--color-graphite: #16181D (secondary surface)
--color-glitch: #7A3CFF (accent only)
--color-glitch-dim: #7A3CFF22
--color-surface: #0A0A0F
--color-surface-elevated: #12131A
--color-border: rgba(255,255,255,0.05)
--color-border-signal: rgba(124,255,79,0.15)
--color-danger: #FF4F4F

TYPOGRAPHY:
--font-body: 'Inter', system-ui, sans-serif
--font-mono: 'JetBrains Mono', monospace
Font scale: xs(12px), sm(14px), base(16px), lg(20px), xl(24px), 2xl(32px), 3xl(48px), 4xl(64px), hero(clamp(48px, 8vw, 96px))

SPACING (8px base unit):
--space-1 through --space-16, plus --space-section(160px)

LAYOUT:
--max-width: 1280px, --grid-columns: 12, --grid-gutter: 24px

MOTION:
--ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1)
--duration-fast: 150ms, --duration-normal: 300ms, --duration-slow: 600ms

BREAKPOINTS (as comments for JS reference):
mobile: 480px, tablet: 768px, desktop: 1024px, wide: 1440px

ELEVATION:
--shadow-sm, --shadow-md, --shadow-lg, --shadow-glow (green)

RADIUS:
--radius-sm(4px), --radius-md(8px), --radius-lg(12px), --radius-xl(16px)

Also create `src/content/site.ts` with brand constants:
- PROJECT_NAME, TICKER, COMMUNITY_NAME ("The Infected"), BLOCKCHAIN
- NAV_LINKS, SOCIAL_LINKS, RANK_NAMES
- CTA_LABELS: { primary: "Join The Infected", secondary: "Read The Signal" }

Also create `src/styles/typography.css` with heading/body/mono classes.
Also create `src/styles/animations.css` with keyframes: glitch-flicker, pulse-glow, scanline-drift, fade-rise, text-reveal.

After implementation: list all tokens, explain file locations, explain consumption pattern.
```

## Prompt 4.2 — UI Component Library

```text
Build the GL1TCH reusable UI component library in `src/components/ui/`.

Components needed:

1. Button.tsx + Button.module.css
   - Variants: primary (green), secondary (ghost), danger
   - Sizes: sm, md, lg
   - States: default, hover (glow), active, disabled, loading
   - Primary hover: box-shadow 0 0 20px var(--color-signal-dim), slight scale(1.02)
   - Include pulse animation on primary CTA buttons

2. Card.tsx + Card.module.css
   - Variants: default, terminal, elevated, interactive
   - Terminal variant: monospace header, green border-left, scanline overlay
   - Interactive: hover lift + glow border

3. Badge.tsx
   - Variants: signal (green), glitch (purple), neutral
   - Small pill shape with subtle glow

4. Accordion.tsx + Accordion.module.css
   - Smooth height animation with Framer Motion
   - Green accent on open state
   - Plus/minus icon transition

5. RankCard.tsx + RankCard.module.css
   - Props: rank name, description, icon, tier level
   - Hover: green glow intensifies based on tier
   - Stacked layout for mobile

6. TimelineCard.tsx + TimelineCard.module.css
   - Phase number, title, items list, status (completed/active/upcoming)
   - Green checkpoint dot, thin connector line

All components must:
- Use CSS Modules (no inline styles for layout)
- Accept className prop for composition
- Have unique IDs for testing
- Be fully responsive
- Use design tokens from tokens.css

Anti-patterns:
- No hardcoded colors — use CSS variables only
- No pixel values for spacing — use space tokens
- No generic border-radius — use radius tokens
```

---

# 5. Website Build Prompts

## Prompt 5.1 — Landing Page Shell

```text
Build the GL1TCH landing page in `src/app/page.tsx`.

Reference: GL1TCH_Founder_OS_v3.md sections 10.1-10.11
Reference: GL1TCH_Website_Wireframe_v2.md

Implement these sections in order:
1. Hero  2. Manifesto  3. Lore Preview  4. Tokenomics
5. Roadmap  6. Community/Ranks  7. Social Proof  8. FAQ  9. Final CTA

Also implement:
- Header: sticky, transparent→solid on scroll, GL1TCH wordmark left, nav center-right, CTA button right
- Footer: wordmark, social links, disclaimer from Founder OS 10.9

Each section component in `src/components/sections/`.
Each pulls content from corresponding `src/content/*.ts` file.
Section spacing: var(--space-section) between major sections.
Max content width: var(--max-width) with auto margins.

Layout: use CSS Grid for 2-column sections, single column on mobile (<768px).

Acceptance Criteria:
- All 9 sections render on desktop and mobile
- Content matches Founder OS sections 10.1-10.11 exactly
- No placeholder Lorem Ipsum — use real GL1TCH copy
- Lighthouse mobile score > 90
- Semantic HTML: single h1, proper heading hierarchy
```

## Prompt 5.2 — Hero Section Premium Upgrade

```text
Refine `src/components/sections/Hero.tsx` to feel truly premium.

Content (from Founder OS 10.1):
- Eyebrow: "SIGNAL DETECTED" (monospace, letter-spacing 0.2em, green)
- Headline: "Infect The Internet." (hero font size, white, bold)
- Subhead: Full supporting copy from section 10.1
- Primary CTA: "Join The Infected" → Telegram link
- Secondary CTA: "Read The Signal" → scrolls to manifesto
- Microcopy: "Built on Solana. Launched for speed. Structured for staying power."

Layout:
- Desktop: 2-column asymmetric — content 7cols left, visual 5cols right
- Mobile: stacked, headline first, CTA full-width

Visual:
- Background: layered — void black base, subtle radial gradient (green 5% opacity center), noise texture overlay (2% opacity)
- Right side: animated signal artifact (CSS-only pulsing concentric circles with green glow)

Motion:
- Headline: text-reveal animation on load (0.6s stagger per word)
- Eyebrow: glitch-flicker once on load
- CTA: pulse-glow infinite subtle animation
- Background circles: slow rotation (60s) + pulse (4s)

Anti-patterns:
- No particle.js or heavy canvas — CSS only for hero effects
- No auto-playing video
- Headline must be readable within 0.5s — animation must not block comprehension
```

## Prompt 5.3 — Manifesto & Lore Sections

```text
Implement Manifesto and Lore sections as the emotional core.

MANIFESTO (src/components/sections/Manifesto.tsx):
- Content from Founder OS 10.2
- Layout: centered text block, max-width 720px
- More negative space than hero — var(--space-section) * 1.5 padding
- Typography: headline 3xl, body lg with 1.8 line-height
- Subtle background: very faint quote fragments at 3% opacity, rotated
- CTA: text link "Witness The Thesis" with underline animation on hover

LORE (src/components/sections/LorePreview.tsx):
- Content from Founder OS 10.3 + lore bible 2.3
- Headline: "It Studied The Feed. Then It Escaped."
- Layout: 2-3 ArchiveCard components in horizontal scroll on mobile, grid on desktop
- Each card: terminal styling, timestamp metadata "ARCHIVE // FILE_0X", green border-left
- Cards use content from src/content/lore.ts
- CTA: "Open Archive" → /lore page
- Subtle static noise overlay on cards (CSS pseudo-element)

Scroll reveal: both sections fade+rise on intersection (threshold 0.2, once).
```

## Prompt 5.4 — Tokenomics, Roadmap, FAQ

```text
Build the trust-layer sections. These should be cleaner and more precise than lore sections.

TOKENOMICS (src/components/sections/Tokenomics.tsx):
- Content from Founder OS 3.6 + 10.4
- Layout: left text (5col), right allocation visual (7col)
- Allocation data from Founder OS 3.6 (50% Public, 20% Community, 15% Marketing, 10% Treasury, 5% Team)
- Visual: horizontal stacked bar chart with green gradient segments, or donut chart via CSS conic-gradient
- Trust badges below: "No Hidden Minting", "Transparency-First", "Multisig Discipline" — each with check icon
- Microcopy about Pump.fun adaptation from 10.4

ROADMAP (src/components/sections/Roadmap.tsx):
- Content from Founder OS 3.10 + 10.5
- Layout: vertical timeline, 4 TimelineCard components
- Phases: Build the Myth → Seed the Network → Launch the Signal → Scale the System
- Each phase has 3-4 bullet items
- Green checkpoint dots, thin connector lines
- Active phase has pulse animation

FAQ (src/components/sections/FAQ.tsx):
- Content from Founder OS 10.10
- Layout: centered, max-width 800px
- Accordion components
- Questions: What is GL1TCH? / Is this a normal meme coin? / Where are official links? / What should users avoid?
- Add 4 more FAQs: How to buy? / What chain? / Is there a team? / What makes GL1TCH different?
- Quiet section — minimal effects, clarity over atmosphere
```

## Prompt 5.5 — Community Ranks & Final CTA

```text
COMMUNITY (src/components/sections/Community.tsx):
- Content from Founder OS 2.4 + 10.6
- Headline: "Not Followers. The Infected."
- Layout: intro text left (4col), rank ladder right (8col)
- 5 RankCard components: Observer → Infected → Signal Bearer → Core Node → Ghost Node
- Each card: rank name, description, desired behavior from Founder OS table 2.4
- Cards glow brighter on hover based on tier (Observer=dim, Ghost Node=bright)
- CTA: "Enter Telegram"

FINAL CTA (src/components/sections/FinalCTA.tsx):
- Content from Founder OS 10.8
- Full-width, dramatic spacing (padding: var(--space-section) * 1.5)
- Headline: "You Can Watch It Spread Or Help It Spread." (3xl-4xl)
- Primary CTA: "Become Infected" (large, pulse-glow)
- Secondary CTA: "View Official Links" (ghost variant)
- Background: subtle radial gradient with green, stronger than other sections
```

## Prompt 5.6 — Motion & Atmosphere Polish

```text
Add motion and atmosphere across the entire website.

GLOBAL EFFECTS (src/components/effects/):
1. Scanlines.tsx — full-viewport CSS pseudo-element, repeating-linear-gradient 2px lines at 3% opacity, slow vertical drift
2. NoiseBackground.tsx — SVG noise filter or CSS noise at 2% opacity, fixed position
3. GlitchText.tsx — reusable component, controlled clip-path animation, 150ms duration, triggers on hover or on-load prop
4. PulseGlow.tsx — wrapper that adds pulsing box-shadow to children

SCROLL REVEALS:
- Create useScrollReveal.ts hook using Intersection Observer
- Default: fade + translateY(20px) → visible, duration 800ms, ease-out-expo
- Threshold: 0.15, triggerOnce: true
- Apply to every section component

HOVER STATES:
- All CTA buttons: scale(1.02) + glow shadow
- Cards: translateY(-2px) + border-color transition to signal-green
- Nav links: underline slide-in from left

PERFORMANCE RULES:
- All animations use transform/opacity only (GPU composited)
- prefers-reduced-motion: disable all motion, show static states
- No animation on elements below fold until intersection
```

## Prompt 5.7 — Official Links & Secondary Pages

```text
BUILD OFFICIAL LINKS PAGE (src/app/links/page.tsx):
- Purpose: single source of truth, reduce fake link risk
- Layout: centered, minimal, max-width 600px
- Include: Official X link, Official Telegram link, Website URL, Contract placeholder
- Safety warning block: "Only trust links from this page and official GL1TCH channels."
- Disclaimer from Founder OS 10.11
- As premium as landing page but cleaner and more minimal

BUILD LORE ARCHIVE PAGE (src/app/lore/page.tsx):
- Extended lore fragments from Founder OS 2.3
- Terminal-style layout: dark cards with green accents
- Each entry: archive number, timestamp, fragment text
- Filterable by category tag
- Atmospheric but readable

BUILD 404 PAGE (src/app/not-found.tsx):
- Brand-consistent error page
- Headline: "SIGNAL LOST" in glitch-flicker
- Subtext: "The transmission you're looking for doesn't exist in this node."
- CTA: "Return to Signal" → home
- Keep the premium atmosphere

BUILD SEO (src/app/layout.tsx):
- Title: "GL1TCH — Infect The Internet | Premium Solana Meme Brand"
- Description from Founder OS hero copy
- OG image placeholder path
- Twitter card meta tags
- Canonical URL
- Structured data: Organization schema
```

---

# 6. Content & Data Layer

## Prompt 6.1 — Structured Content System

```text
Create typed content files so all GL1TCH copy is centralized and editable.

TYPE DEFINITIONS (src/types/content.ts):
- HeroContent, ManifestoContent, LoreEntry, TokenAllocation, RoadmapPhase, RankDefinition, FAQItem, CTABlock, SocialLink, FooterContent

CONTENT FILES (src/content/):
All content sourced from GL1TCH_Founder_OS_v3.md sections 10.1-10.11, 2.3, 2.4, 3.6.

Each file exports typed const. Example:
hero.ts → exports heroContent: HeroContent
faq.ts → exports faqItems: FAQItem[]
lore.ts → exports loreEntries: LoreEntry[] (minimum 6 fragments)

Requirements:
- Type-safe with TypeScript
- Easy to edit without touching components
- Extensible for future i18n (TR/EN)
- All CTA labels centralized in site.ts
```

## Prompt 6.2 — Lore Fragment System

```text
Create a reusable lore fragment data system in src/content/lore.ts.

Structure each entry with: id, archiveNumber, timestamp, title, body, category, tone.

Categories: origin, transmission, system-alert, prophecy, corruption-log

Create minimum 12 fragments using Founder OS 2.3 lore language.
Include 3 "fake system alerts" in the style of Founder OS content day 7.

These should be consumable by: website lore page, lore preview section, and future Telegram bot /lore command.
```

---

# 7. Telegram Bot

## Prompt 7.1 — Bot v1

```text
Implement GL1TCH Telegram bot using grammy (TypeScript) in a /bot directory.

Structure:
bot/
├── src/
│   ├── index.ts
│   ├── commands/ (start, rank, lore, faq, links, rules, raid, submit, support)
│   ├── admin/ (announce, warn, mute, ban, pin, incident)
│   ├── middleware/ (antiSpam, adminCheck, logging)
│   ├── content/ (shared content from website or imported)
│   └── utils/
├── .env.example (BOT_TOKEN, ADMIN_IDS, GROUP_ID)
├── package.json
└── tsconfig.json

Commands from Founder OS 7.3. Onboarding flow from 7.4.
Brand-consistent tone throughout.
Fallback: "Unknown signal. Use /faq or /links for verified guidance."
Anti-spam: rate limiting, scam phrase detection, fake admin detection.
Admin commands: protected by middleware, logged.
```

---

# 8. Dashboard

## Prompt 8.1 — Founder Dashboard v1

```text
Build a founder-facing dashboard at /dashboard (protected route or separate deployment).

KPIs from Founder OS 5.11:
- X follower growth, impressions, TG joins, active posters, meme submissions

Visual: premium dark theme matching website. Cards with metric, trend indicator, sparkline.
Campaign tracking structure for content campaigns, creator outreach, meme contests.
Mock-safe: use static data that can later connect to real APIs.
```

---

# 9. Web3 Integration

## Prompt 9.1 — Solana Wallet Connection

```text
Add optional Solana wallet connection to the website.

Use @solana/wallet-adapter-react with Phantom, Solflare, Backpack.
Create WalletProvider wrapper in src/components/layout/WalletProvider.tsx.
Add "Connect Wallet" button in header (subtle, secondary style).
When connected: show truncated address, green "connected" badge.

This is NOT for trading. It's for future holder verification and rank features.
Do not make wallet connection required for any current feature.
```

---

# 10. Token Creation & On-Chain Launch (CORE)

This is the actual coin. Everything else supports it. The chosen strategy is **two-phase**: launch on **Pump.fun** for a fair, fast, low-friction start, with a planned, optional **migration to a self-managed Raydium/Meteora LP** once volume and community justify deeper liquidity control. Build the tooling for both paths up front so a migration is never a scramble.

> Safety-first principle: every on-chain action is rehearsed on **devnet** first, scripted (never click-only), and the resulting addresses/signatures are recorded to `src/lib/official.ts` and the launch log. No manual one-off transactions from the founder's main wallet.

## Prompt 10.0 — On-Chain Workspace & Wallet Setup

```text
Create an `onchain/` workspace (separate package, NOT bundled into the Next.js app) for all Solana token operations.

Structure:
onchain/
├── src/
│   ├── config.ts        (cluster selection: devnet | mainnet-beta, RPC from env)
│   ├── wallets.ts       (load keypairs from env/file path — NEVER hardcode secrets)
│   ├── token/
│   │   ├── createMint.ts        (SPL token mint creation, Token-2022 vs legacy decision documented)
│   │   ├── metadata.ts          (Metaplex token metadata: name, symbol, image, description)
│   │   ├── mintSupply.ts        (mint full supply to treasury)
│   │   ├── revokeAuthorities.ts (revoke mint + freeze authority)
│   │   └── verify.ts            (read on-chain state, assert authorities are null)
│   ├── liquidity/
│   │   ├── raydiumPool.ts       (create CPMM/standard pool)
│   │   ├── meteoraPool.ts       (alt: DLMM pool)
│   │   └── burnLp.ts            (burn or time-lock LP tokens)
│   └── pumpfun/
│       └── launchNotes.md       (Pump.fun launch checklist — UI-driven, documented step list)
├── scripts/ (runnable: devnet-dry-run, mainnet-launch, post-launch-verify)
├── .env.example (SOLANA_CLUSTER, RPC_URL, TREASURY_KEYPAIR_PATH, ...)
├── package.json (@solana/web3.js, @solana/spl-token, @metaplex-foundation/*, raydium SDK)
└── README.md (runbook: devnet dry-run → mainnet launch → verify)

Requirements:
- TypeScript, strict mode.
- Cluster is env-driven; default devnet; mainnet requires explicit SOLANA_CLUSTER=mainnet-beta + a confirmation flag.
- Secrets NEVER committed. Add onchain/.env and keypair files to .gitignore.
- Every script prints: cluster, fee payer, expected effect, and waits for confirmation before sending on mainnet.
- All token parameters (name, symbol, decimals, supply) imported from a single config sourced from Founder OS 3.6 — no divergence.

Acceptance Criteria:
- `npm run devnet-dry-run` completes a full create→mint→revoke→pool→burn cycle on devnet and prints all addresses.
- No secret material in the repo.

Anti-patterns:
- Do NOT put on-chain private keys in the Next.js app or any NEXT_PUBLIC_ var.
- Do NOT perform irreversible mainnet actions without an explicit confirm step.
- Do NOT hardcode supply/decimals — read from Founder OS-derived config.
```

## Prompt 10.1 — Path A: Pump.fun Launch Runbook

```text
Create onchain/src/pumpfun/launchNotes.md — a precise, step-by-step Pump.fun launch runbook (Pump.fun is UI-driven, so this is a checklist + verification script, not an automated mint).

Include:
- Pre-launch: wallet funded with SOL for buy, token name/symbol/description/image finalized from Founder OS, X + TG links ready.
- Token metadata exactly matching brand: name, ticker (TICKER from site.ts), image (OG-quality, on-brand).
- Initial dev buy strategy: documented, small, transparent — explicitly NO covert large dev allocation (anti-rug stance from Founder OS 3.11).
- Anti-snipe posture: how the contract address is revealed (simultaneous across X/TG/site /links page) to reduce sniping advantage.
- Immediately-after steps: grab the contract address (CA), paste into src/lib/official.ts CONTRACT_ADDRESS, flip LAUNCH_STATUS to LIVE, publish on /links, pin in TG, post on X.
- Migration note: Pump.fun auto-migrates to Raydium at its bonding-curve threshold — document what that means for liquidity and what the team does NOT need to do manually.
- Verification: a `scripts/post-launch-verify.ts` call that confirms the mint's authorities and supply on-chain and that the CA on /links matches.

Anti-patterns:
- No hidden team wallets sniping their own launch.
- No promising price action.
```

## Prompt 10.2 — Path B: Self-Managed SPL Mint + Raydium/Meteora LP

```text
Implement the self-managed launch path in onchain/src/token/ and onchain/src/liquidity/ for the scenario where GL1TCH runs its own mint + pool (either as the primary launch or as a post-Pump.fun scale-up).

Token creation (createMint.ts, metadata.ts, mintSupply.ts):
- Decide and DOCUMENT legacy SPL Token vs Token-2022. Default to legacy SPL Token unless a Token-2022 extension (e.g. transfer fee) is explicitly required — many DEXs/tools have limited Token-2022 support. State the tradeoff in code comments.
- Decimals and total supply from Founder OS 3.6.
- Metaplex metadata: name, symbol, URI (points to hosted JSON with image + description). Host metadata JSON and image on a permanent/pinned store (Arweave/IPFS) — document the chosen store.
- Mint full supply to the treasury multisig (see Section 11), not a hot wallet.

Liquidity (raydiumPool.ts / meteoraPool.ts):
- Create the pool with the allocation reserved for public liquidity per Founder OS 3.6.
- Print pool ID, LP mint, and initial price.

LP handling (burnLp.ts):
- Default: burn LP tokens to prove liquidity can't be pulled (rug-proof), OR time-lock via a reputable locker — make this a documented, deliberate choice, not an afterthought.

Acceptance Criteria:
- Full devnet dry-run produces a tradeable pool.
- post-launch-verify confirms: mint authority = null, freeze authority = null, LP burned/locked as configured.

Anti-patterns:
- Do NOT skip the devnet rehearsal.
- Do NOT leave LP tokens sitting in a hot wallet post-launch.
```

---

# 11. Token Security & Treasury (GATE — must pass before mainnet)

A meme coin lives or dies on verifiable trust. These steps are non-negotiable and must be **proven on-chain**, not just claimed in copy.

## Prompt 11.1 — Authority Hardening & Verification

```text
Implement onchain/src/token/revokeAuthorities.ts and verify.ts and wire them into the launch scripts.

Required guarantees, each asserted programmatically:
1. Mint authority revoked (set to null) after full supply is minted — no more tokens can ever be created.
2. Freeze authority revoked (set to null) — the team can never freeze holder wallets.
3. (If self-managed LP) LP tokens burned or locked, with the tx signature recorded.
4. Update authority on metadata: decide whether metadata is made immutable; document the choice.

verify.ts must:
- Read live on-chain mint state and FAIL LOUDLY if any authority is non-null when it should be null.
- Output a human-readable "Trust Report" (✓/✗ per guarantee) plus the explorer links.
- Be runnable any time post-launch so the community/team can re-verify.
- Capture external verification: link/score from RugCheck.xyz and/or SolSniffer for the contract (FLOKI leans on CertiK; our equivalent for a Solana token is RugCheck/SolSniffer). Record the score and URL.

Wire the Trust Report results into:
- src/lib/official.ts (machine-readable booleans the website can display).
- The website Tokenomics section trust badges (Section 5.4) — badges reflect REAL verified state, not hardcoded claims.

Anti-patterns:
- Do NOT display "Mint Revoked ✓" on the site unless verify.ts confirms it on-chain.
- Do NOT revoke authorities before the full supply is minted.
```

## Prompt 11.2 — Treasury Multisig & Key Management

```text
Document and script the treasury/multisig setup in onchain/ (README + helper scripts).

Requirements:
- Treasury is a multisig (e.g. Squads) — NOT a single founder hot wallet. Document signer set, threshold (e.g. 2-of-3), and who holds keys.
- Full supply (Path B) or any team/treasury allocation is minted/sent to the multisig.
- Define cold vs hot wallet usage: launch operations use a dedicated hot wallet funded with only what's needed; long-term holdings sit in the multisig/cold storage.
- Key handling rules: hardware wallet for signers, no plaintext seed phrases, no keys in cloud notes, env files gitignored.
- A written incident plan: what to do if a signer key is compromised.

Output: onchain/SECURITY.md covering wallet topology, signer responsibilities, and the incident plan.

Anti-patterns:
- No single point of failure for treasury funds.
- No private keys anywhere in version control or NEXT_PUBLIC_ env.
```

## Prompt 11.3 — Pre-Mainnet Security Checklist

```text
Create onchain/PRE_LAUNCH_CHECKLIST.md — a hard gate that must be 100% checked before any mainnet transaction.

Sections:
- Devnet rehearsal completed end-to-end (link to dry-run output).
- Token params match Founder OS 3.6 exactly (supply, decimals, symbol, name).
- Metadata image + JSON hosted permanently and reachable.
- Authorities revocation script tested on devnet.
- Treasury multisig live and signers confirmed.
- LP burn/lock decision made and scripted.
- Contract-address reveal plan ready (simultaneous publish across channels).
- official.ts ready to update; /links page deploys CA instantly.
- Legal/risk disclaimers live (Section 12).
- Rollback/incident plan reviewed.

Each item: owner + status. The doc states clearly: do not proceed to mainnet with any unchecked item.
```

---

# 12. Legal, Compliance & Risk

Meme coins carry real legal and reputational risk. This layer protects the project and its community and reduces scam ambiguity.

## Prompt 12.1 — Risk, Disclaimer & Compliance Copy

```text
Create src/content/legal.ts and a /disclaimer route (or expand /links) sourcing Founder OS section 12.

Include:
- "Not financial advice" disclaimer, high-risk warning, no-guaranteed-returns language.
- Regional caution note (users responsible for local law compliance; no offering where prohibited).
- Clear statement that GL1TCH is a community/brand/meme project, not a security or investment contract — describe utility honestly, make no profit promises.
- Impersonation & scam policy: only official channels in official.ts are real; how to report fakes.
- Privacy note if any analytics/cookies are used (tie to NEXT_PUBLIC_GA_ID usage).

Surface this copy in: footer, /links, /disclaimer, FAQ, and the Telegram bot /rules and /links commands — single source so wording never diverges.

Anti-patterns:
- No language implying guaranteed profit or "investment".
- No copying a generic disclaimer that doesn't match the actual project.
```

## Prompt 12.2 — On-Chain Transparency & Anti-Scam Surface

```text
Build the public trust/transparency surface so holders can independently verify the project.

Requirements:
- On /links and Tokenomics: live links to the token on Solscan/Solana Explorer, DEXScreener, and Pump.fun (post-launch).
- **Single consolidated Trust Wall** (benchmark: beat FLOKI's CertiK panel): mint revoked ✓, freeze revoked ✓, LP burned/locked ✓, external RugCheck/SolSniffer score + link, holder count, FDV ≈ market cap note — all on one screen, all from real state (verify.ts + analytics from Prompt 15.5), never hardcoded.
- A prominent "Verify Yourself" block explaining how to check the contract address and authorities.
- Impersonation warning banner driven by LAUNCH_STATUS (e.g. during PRE_LAUNCH: "No contract exists yet. Anyone selling GL1TCH now is a scam.").
- **Social-proof wall** (Section 5.7): media/partner/exchange-listing badges — placeholder slots that fill in as real coverage lands; never fake a logo we don't have.
- **Give-back transparency**: a link to the public on-chain give-back wallet (Solscan) so anyone can verify funds and outflows — beats opaque charity claims.

This directly reduces the scam-ambiguity risk called out in the wireframe acceptance criteria.
```

---

# 13. Launch Tooling

## Prompt 13.1 — Launch Content Pack

```text
Create src/content/launch/ directory with:
- announcement.ts: launch post copy variants
- followups.ts: first 12-hour follow-up posts
- safety.ts: scam warnings, official link reminders
- cta-variants.ts: different CTA copy for different channels
- faq-launch.ts: launch-specific FAQ additions

All content from Founder OS sections 5.9, 6.3 (days 25-27).
```

## Prompt 13.2 — Source of Truth Module

```text
Create src/lib/official.ts as the centralized registry:
- OFFICIAL_X_URL, OFFICIAL_TG_URL, OFFICIAL_SITE_URL
- CONTRACT_ADDRESS (placeholder until launch)
- EXPLORER_URL, DEXSCREENER_URL, PUMPFUN_URL (derived from CONTRACT_ADDRESS once set)
- TRUST_REPORT object: { mintRevoked, freezeRevoked, lpBurnedOrLocked } — populated from onchain/verify.ts output, NOT hardcoded true
- DISCLAIMER text from Founder OS 10.11 / 12
- WARNING text for impersonation alerts (status-aware)
- LAUNCH_STATUS enum: PRE_LAUNCH | LAUNCHING | LIVE | POST_LAUNCH

This module is the single source the website, bot, and dashboard all import for official links, launch state, and verified trust badges. Updating launch state = editing this one file.
```

---

# 14. DevOps & Deployment

## Prompt 14.1 — Deployment Setup

```text
Configure deployment for Vercel:
- vercel.json with headers (security, caching)
- .env.example with all required vars: NEXT_PUBLIC_SITE_URL, NEXT_PUBLIC_GA_ID, BOT_TOKEN, ADMIN_IDS
- next.config.js: image domains, security headers, redirects
- robots.txt and sitemap generation
- CI: GitHub Actions for lint + type-check on PR
```

---

# 15. QA & Refinement

## Prompt 15.1 — Brand Consistency Audit

```text
Review the entire GL1TCH project against GL1TCH_Founder_OS_v3.md.
Audit: tone, visual direction, CTA clarity, mobile quality, premium feel, trust signals.
Do not praise — identify specific weak points and fixes.
```

## Prompt 15.2 — Performance Audit

```text
Run Lighthouse audit and optimize for:
- LCP < 2.5s, CLS < 0.1, FCP < 1.8s
- Bundle size analysis — identify and fix heavy imports
- Image optimization: WebP, lazy loading, responsive srcset
- Font loading: display=swap, preload critical fonts
- prefers-reduced-motion support verification
```

## Prompt 15.3 — Launch Readiness

```text
Review for launch: official links clarity, FAQ completeness, scam-risk reduction, mobile usability, bot readiness, dashboard usefulness.
List blockers, risks, and priorities.
```

## Prompt 15.4 — Automated Testing

```text
Add an automated test layer so regressions are caught before deploy.

Website (Vitest + React Testing Library):
- Unit tests for content rendering (each section renders Founder OS copy, no empty states).
- Tests asserting trust badges reflect official.ts values (not hardcoded).
- Accessibility smoke test (axe) on home, /lore, /links, 404.

E2E (Playwright):
- Critical paths: CTAs link to correct TG/X, /links shows CONTRACT_ADDRESS when LIVE, mobile sticky CTA bar appears/disappears correctly.
- prefers-reduced-motion: animations disabled.

On-chain (onchain/ tests):
- Run verify.ts against a devnet mint and assert the Trust Report (authorities null, supply correct).

CI: wire all of the above into the GitHub Actions workflow from Prompt 14.1 (lint + type-check + unit + e2e on PR; on-chain tests against devnet on demand).

Anti-patterns:
- No tests that assert hardcoded trust state — they must read real config/chain.
```

## Prompt 15.5 — On-Chain & Growth Analytics

```text
Wire real post-launch metrics into the founder dashboard (Section 8) and the public transparency surface.

On-chain:
- Holder count, top-holder concentration, volume, market cap, liquidity — via DEXScreener / Birdeye / Solscan APIs (server-side, keys in env).
- Surface concentration warnings internally (e.g. if top wallet > X%).

Growth (already-defined KPIs from Founder OS 5.11):
- X follower growth, impressions, TG joins, active posters, meme submissions.

Implementation:
- Server-side fetchers with caching/rate-limit respect; never expose private API keys client-side.
- Dashboard switches from mock data (Prompt 8.1) to live sources behind a feature flag.

Anti-patterns:
- No private analytics API keys in NEXT_PUBLIC_ vars.
- No blocking the public site on third-party API latency — cache and degrade gracefully.
```

---

# 16. Dependency Graph

```
Prompt 2A.0 (Founder OS Bootstrap) ← ABSOLUTE FIRST — everything depends on it
  └── Prompt 3.0 (Init Project)
       └── Prompt 4.1 (Design Tokens)
            └── Prompt 4.2 (UI Components)
                 └── Prompt 5.1 (Landing Shell)
                      ├── Prompt 5.2 (Hero)
                      ├── Prompt 5.3 (Manifesto + Lore)
                      ├── Prompt 5.4 (Tokenomics + Roadmap + FAQ)
                      ├── Prompt 5.5 (Community + Final CTA)
                      └── Prompt 5.6 (Motion Polish)
                 └── Prompt 5.7 (Secondary Pages)
            └── Prompt 6.1 (Content System) ← can run parallel with 5.x
                 └── Prompt 6.2 (Lore Fragments)

Prompt 7.1 (Bot) ← independent after 6.1
Prompt 8.1 (Dashboard) ← independent after 4.1

ON-CHAIN TRACK (the actual coin — can start in parallel after 2A.0):
Prompt 10.0 (On-chain workspace + wallets)
  └── Prompt 10.1 (Pump.fun runbook)  AND  Prompt 10.2 (Self-managed mint + LP)
       └── Prompt 11.1 (Authority hardening + verify)
            └── Prompt 11.2 (Treasury multisig)
                 └── Prompt 11.3 (Pre-mainnet checklist) ← HARD GATE
Prompt 12.1-12.2 (Legal + transparency) ← required before 11.3 passes
Prompt 9.1 (Web3 wallet) ← after 5.1
Prompt 13.1-13.2 (Launch tooling + official.ts) ← after 6.1; 13.2 consumed by on-chain track
Prompt 14.1 (Deploy) ← after 5.x complete
Prompt 15.1-15.5 (QA + tests + analytics) ← after all above

LAUNCH GATE: do not execute mainnet (10.1/10.2 on mainnet) until
11.1, 11.2, 11.3, 12.1, 12.2 are all complete and verified.
```

---

# 17. Environment Variables

```env
# Public (safe to expose — bundled into client)
NEXT_PUBLIC_SITE_URL=https://gl1tch.io
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_SOLANA_RPC=https://api.mainnet-beta.solana.com
NEXT_PUBLIC_CONTRACT_ADDRESS=        # filled at launch from official.ts
NEXT_PUBLIC_GIVEBACK_WALLET=         # public Solana give-back wallet (verifiable on Solscan)

# Private (server/bot only — NEVER prefix with NEXT_PUBLIC_)
BOT_TOKEN=
ADMIN_IDS=
GROUP_ID=
DEXSCREENER_API_KEY=
BIRDEYE_API_KEY=

# On-chain workspace (onchain/.env — gitignored, NEVER committed)
SOLANA_CLUSTER=devnet                # devnet until launch; mainnet-beta requires explicit confirm
RPC_URL=https://api.devnet.solana.com
TREASURY_KEYPAIR_PATH=               # path to keypair file, never the key itself
FEE_PAYER_KEYPAIR_PATH=
TOKEN_NAME=GL1TCH
TOKEN_SYMBOL=                        # TICKER — from Founder OS 3.6
TOKEN_DECIMALS=
TOKEN_TOTAL_SUPPLY=
METADATA_URI=                        # Arweave/IPFS JSON URI
CONFIRM_MAINNET=false                # must be true to send mainnet transactions
```

Secret-handling rule: anything that can move funds or post as the brand (keypairs, BOT_TOKEN, API keys) is server/CLI-only and gitignored. The only on-chain value safe for the client is the public contract address.

---

# 18. Emergency Prompts

## Fix: Site looks generic
```text
The site currently looks too generic/template-like. Review against GL1TCH_Founder_OS_v3.md and fix: add more dramatic spacing, replace any default colors with token colors, add controlled glitch effects, increase typography contrast, add scanline/noise atmosphere. The result must feel like a cinematic rogue AI terminal.
```

## Fix: Mobile is broken
```text
Mobile experience is degraded. Fix: stack all 2-column layouts below 768px, make CTAs full-width, ensure text is readable without zoom, add sticky bottom CTA bar with "Join TG" and "Follow X", reduce decorative motion on mobile, test touch targets minimum 44px.
```

## Fix: Performance is slow
```text
Site is slow. Optimize: lazy load below-fold sections with dynamic imports, convert images to WebP, add font-display: swap, defer non-critical CSS animations, add loading="lazy" to images, check and reduce JavaScript bundle size, implement ISR for content pages.
```

---

# 19. Final Quality Gate

Before considering the project launch-ready, every item must pass.

**Brand & Website**
- [ ] First screen instantly feels premium and distinct (not template-like)
- [ ] Visitor understands GL1TCH in under 10 seconds
- [ ] Telegram and X CTAs are impossible to miss
- [ ] Tokenomics and trust surfaces reduce scam ambiguity
- [ ] Visual language matches Founder OS consistently
- [ ] Mobile version has equal emotional power
- [ ] Lighthouse mobile score > 90
- [ ] All content matches Founder OS — no invented messaging
- [ ] prefers-reduced-motion respected
- [ ] Official links page is clear and minimal
- [ ] 404 page is brand-consistent
- [ ] All interactive elements have unique IDs
- [ ] No hardcoded colors, spacing, or fonts — all from tokens

**Foundation**
- [ ] `GL1TCH_Founder_OS_v3.md` exists, complete, no unresolved {{placeholders}} except live launch values
- [ ] Automated tests (unit + e2e) pass in CI

**Token Launch (CORE — coin cannot ship without these)**
- [ ] Full create→mint→revoke→pool→burn cycle rehearsed on devnet
- [ ] Token params (supply, decimals, symbol, name) match Founder OS 3.6 exactly
- [ ] Metadata image + JSON hosted permanently (Arweave/IPFS) and reachable
- [ ] **Mint authority revoked** — verified on-chain by verify.ts
- [ ] **Freeze authority revoked** — verified on-chain by verify.ts
- [ ] LP burned or time-locked (self-managed path) — tx signature recorded
- [ ] Treasury is a multisig; no single-key control of funds
- [ ] No private keys in repo or NEXT_PUBLIC_ vars
- [ ] Trust badges on site reflect real verify.ts output, not hardcoded
- [ ] Contract-address reveal plan ready (simultaneous across X/TG/site)
- [ ] official.ts CONTRACT_ADDRESS + LAUNCH_STATUS update flow tested

**Legal & Risk**
- [ ] Disclaimer / not-financial-advice / high-risk copy live on site + bot
- [ ] Impersonation/scam policy published; status-aware warning banner works
- [ ] Public "Verify Yourself" + explorer/DEXScreener links present
- [ ] `onchain/PRE_LAUNCH_CHECKLIST.md` is 100% checked

**Competitive Bar (beat FLOKI — Section 2B)**
- [ ] Zero buy/sell tax confirmed on-chain
- [ ] External RugCheck/SolSniffer score published on the Trust Wall
- [ ] Single consolidated Trust Wall live (authorities + LP + holders + audit + FDV note)
- [ ] Exactly ONE flagship utility defined and scoped (no sprawl)
- [ ] Competitive positioning ("not FLOKI because…") reflected in copy/tone
- [ ] Social-proof wall present (real badges only, no fakes)
- [ ] Multi-platform content + X Spaces/raid cadence documented
- [ ] Flagship utility (holder-gated content + ranks) scoped and deliverable
- [ ] Give-back wallet live, public, linked on Trust Wall (on-chain verifiable)

> Hard rule: if any Token Launch or Legal item is unchecked, **do not execute the mainnet launch.**
